import { FilterDepartmentActivitiesPipe } from './filter-department-activities-pipe';

describe('FilterDepartmentActivitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDepartmentActivitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
